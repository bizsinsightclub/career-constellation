# design.md — 커리어 별자리 디자인 가이드라인

## 1. 아트 디렉션 선언

**"밤의 점술가가 펼친 카드 한 장, 그 안에 담긴 하늘."**

방향은 고딕 성당이 아니라 타로·오컬트다. 참고 축은 세 가지: ① 고전 타로 카드(라이더-웨이트, 마르세유)의 목판 선각화와 카드 프레임 ② 19세기 오컬트 서적의 천구도·연금술 도해 ③ Path of Exile 패시브 트리의 밀도와 관성. 게임·카드 원본 이미지는 사용하지 않고 분위기만 취하며, 모든 시각 자산은 SVG로 자체 제작한다.

핵심 은유: **화면 전체가 한 장의 카드다.** 사용자의 커리어는 카드 안에 그려진 천구도이고, 해석은 그 카드를 읽어주는 리딩이다.

무드 키워드: 신비 / 촛불 / 금박 선각화 / 낡은 천구도 / 점술사의 탁자

## 2. 컬러 토큰

| 토큰 | 값 | 용도 |
|---|---|---|
| `--void` | `#0D0A16` | 기저 배경 — 자줏빛이 도는 흑청색 (순흑 금지) |
| `--veil` | `#1E1533` | 배경 상층 그라디언트, 패널 바탕 (짙은 보라의 장막) |
| `--socket-dim` | `#463A5E` | 미점등 소켓, 천구도 눈금선 |
| `--star-candle` | `#C77F35` | tier 1 — 촛불빛 호박색 |
| `--star-gilt` | `#E6BE6A` | tier 2 — 금박 |
| `--star-moon` | `#F2EDDF` | tier 3 — 달빛 백금색 (외곽은 gilt로 번짐) |
| `--lunar` | `#B9BFDD` | 은빛 보조색 — 간선 발광, 달 위상 심볼 |
| `--bone` | `#DDD2BC` | 해석 본문 텍스트 (오래된 카드지 색) |
| `--oxblood` | `#6E2438` | 경고·삭제 등 극히 제한적 사용 |

원칙: 유채색 축은 「금(candle→gilt→moon)」과 「은(lunar)」 둘뿐. 금은 사용자의 빛(경력), 은은 세계의 구조(선·심볼·눈금)를 맡는다. 이 역할 분담을 절대 섞지 않는다.

## 3. 타이포그래피

| 역할 | 서체 | 비고 |
|---|---|---|
| 표제 (라틴) | Cinzel Decorative | 카드 이름·아르카나 번호 — 장식 획이 타로 카드 표제와 통함 |
| 표제 (한글) | 마루 부리 (MaruBuri) | Cinzel Decorative와 병기 시 자간 +0.05em |
| 해석 본문 | Noto Serif KR Light | 행간 1.9 — 점술사가 낭독하듯 느리게 읽히는 밀도 |
| 데이터·라벨 | Pretendard | 노드 툴팁, 점수 등 기능 텍스트만 |

- 아르카나 번호는 로마 숫자로 표기 (예: XXII)
- 본문은 순백(`#FFF`) 금지 — `--bone` 사용
- 해석문 도입부에 장식 이니셜(드롭캡), 문단 구분은 줄바꿈 대신 작은 별 심볼(✦ 형태의 자체 SVG) 중앙 배치

## 4. 공간 연출 (배경)

- 3중 레이어: ① `--void` 기저 ② 중앙 상단에서 내려오는 `--veil` 방사형 그라디언트 ③ 미세 노이즈(투명도 3%, 종이 질감의 암시)
- **천구도 구조**: 지도 뒤에 `--socket-dim` 투명도 15%의 동심원 3~4개와 방사 눈금을 아주 희미하게 깔아, 별들이 점성술 차트 위에 놓인 것처럼 보이게 한다
- **카드 프레임 (시그니처)**: 화면 가장자리에 이중 금선(1px + 0.5px) 테두리와 네 모서리의 오컬트 문양(달 위상·눈·손·열쇠 — 선각화 스타일 SVG 4종 자체 제작). 화면 전체가 한 장의 카드임을 시각적으로 선언하는 장치
- 미점등 영역에 배경 잔별(1px, 투명도 5~8%)을 흩뿌려 "아직 읽히지 않은 하늘"을 암시

## 5. 소켓과 별 — 상태 정의

| 상태 | 크기 | 시각 |
|---|---|---|
| 미점등 소켓 | 10px | `--socket-dim` 1px의 「원 + 내부 점 없음」 — 천문 기호의 빈 홈. 빛이 깃들 자리임이 보여야 함 |
| tier 1 | 14px | 소켓 안에 `--star-candle` 점광 — 촛불이 켜진 듯. blur 4px |
| tier 2 | 20px | `--star-gilt` + 4갈래 빛살(십자 광조). blur 8px |
| tier 3 | 28px | `--star-moon` 중심 + gilt 이중 광륜 + 8갈래 빛살. blur 14px, 3초 주기의 느린 맥동 |

- 글로우는 SVG `feGaussianBlur` 필터로 구현
- 클러스터 중심 노드는 각 대분류를 상징하는 **선각화 심볼**로 구분: 전략=눈, 창작=손, 분석=천칭, 리더십=왕관, 실행=열쇠, 학습=펼친 책, 산업=탑, 성과=월계 — 전부 획 1.5px 라인 SVG 자체 제작 (기성 아이콘·이모지 금지)
- 심볼은 미점등 시 `--socket-dim`, 하위 노드가 하나라도 점등되면 `--lunar`로 발광

## 6. 간선 (별자리 선)

- 미점등: `--socket-dim`, 0.5px, 투명도 30%
- 양끝 점등 시: `--lunar` 1px 은빛 발광 (금이 아니라 은 — 2장의 역할 분담)
- 점등 순간 선이 한쪽 별에서 다른 별로 흘러가듯 그려짐 (stroke-dashoffset 애니메이션, 0.8초)

## 7. 모션 원칙

- **리딩의 3막 구조가 유일한 큰 연출이다:**
  1. 분석 시작 — 화면이 살짝 어두워지고 배경 천구도 눈금이 한 바퀴 느리게 회전 (카드를 섞는 시간)
  2. 점등 — 해당 소켓들이 0.15초 간격으로 순차 점화. 각 점화는 「섬광(0.2초) → 수축 → 안정 광도」
  3. 카드 공개 — 해석 패널이 뒤집히는 카드처럼 등장 (Y축 flip, 0.6초)
- 안정 상태의 별은 3~5초 주기 무작위 위상의 느린 맥동만 (일제히 깜빡이면 인공적임)
- 지도 이동·확대는 감속 곡선(ease-out)으로 묵직하게
- `prefers-reduced-motion` 시 회전·점화·뒤집힘 연출 제거, 최종 상태만 표시

## 8. UI 컴포넌트

### API 키 입력 화면 (첫 진입)
- 카드 프레임 안에 짧은 안내와 입력창 하나만. "열쇠를 꽂아야 카드를 읽을 수 있습니다" 톤의 한 줄 + 기능적 설명("Google AI Studio에서 무료로 발급 · 키는 이 브라우저에만 저장됩니다")
- 세계관 문구와 기능 설명을 한 화면에 두되, 버튼·안내는 명확한 우리말로

### 입력 패널
- 하단 고정. `--veil` 반투명 판 위에 `--bone` 텍스트
- 플레이스홀더: "그대의 여정을 들려주십시오" 톤. 실행 버튼 라벨은 "별빛 깃들이기" 하나만 세계관 용어 허용, 나머지는 "지우기", "다시 입력" 등 기능 그대로

### 해석 패널 — 타로 카드 (시그니처 연출)
- 세로 비율 약 3:5의 카드 형태. 처음엔 카드 뒷면(어둠 + 은빛 문양의 뒷면 패턴 SVG)이 보이고, 리딩 완료 시 뒤집힘
- 앞면 구성: 상단에 로마 숫자 + 아르카나 이름(Cinzel Decorative + 마루부리, 중앙 정렬) → 드롭캡으로 시작하는 해석 본문 → 하단 "다음에 깃들 별" 목록(해당 미점등 노드로 클릭 이동)
- 테두리는 이중 금선 + 네 모서리 문양 — 화면 프레임과 같은 문법의 축소판
- 양피지·카드지 질감 이미지를 깔지 말 것. 어둠 + 금선 + 타이포그래피만으로 카드 느낌을 낸다
- 모바일: 카드가 전체 화면으로 전환

### 툴팁
- 노드 호버 시: 노드명, tier, 근거(evidence) 1줄. Pretendard, `--void` 판 + `--socket-dim` 테두리

## 9. 절제 규칙

- 화려함의 예산은 **카드가 뒤집히는 순간** 한 곳에만 쓴다. 나머지는 전부 조용하게
- 장식을 추가하고 싶을 때마다 자문: "이것이 카드 한 장의 고요함을 훼손하는가?" 훼손하면 뺀다
- 오컬트 클리셰 경계: 펜타그램·해골 등 어둡고 위협적인 기호는 쓰지 않는다. 이 서비스의 점술은 불길함이 아니라 **가능성을 읽어주는 다정한 리딩**이다 — 심볼은 눈·달·손·열쇠·책처럼 열려 있는 것들로만
- 아이콘이 필요하면 획 1.5px 라인 선각화 스타일로 자체 제작 — 이모지·기성 아이콘 세트 금지
