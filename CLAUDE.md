# CLAUDE.md — 커리어 별자리 (Career Constellation)

## 1. 프로젝트 개요

이력서 또는 자연어로 서술한 경력 내용을 입력하면, Path of Exile의 패시브 트리처럼 복잡하게 얽힌 별자리 형태의 소켓 지도 위에 해당 경력이 "별빛"으로 깃드는 서비스.

- 입력: 이력서 텍스트, 또는 자유 서술("저는 8년간 건설사에서 브랜드 전략을…")
- 처리: LLM이 서술을 분석해 소켓 지도의 노드에 매핑 (일종의 커리어 빙고)
- 출력 1: 별자리 시각화 — 언급 빈도·경력 깊이에 따라 별의 크기와 광도가 달라짐
- 출력 2: 별자리 해석 — 점성술 풀이 같은 감성적 문체로 커리어의 의미, 잠재력, 다음에 이어질 별(추천 경로)을 서술

## 2. 개발 환경

- OS: Windows / 터미널: VS Code 통합 PowerShell (명령어는 PowerShell 문법으로 안내할 것. `&&` 대신 `;` 사용)
- 사용자는 비개발자이므로:
  - 모든 설명·주석은 한국어로 작성
  - 코드 변경 후 반드시 실행 방법을 한 줄로 안내
  - 한 번에 하나의 기능만 구현하고 확인 후 진행
- 콩글리쉬·외래어 남용 금지. 단 고유 기술명(React, SVG 등)은 원문 유지

## 3. 기술 스택 (기본 가정 — 변경 가능)

| 영역 | 선택 | 이유 |
|---|---|---|
| 프런트엔드 | Vite + React | 단일 페이지, 빠른 개발 |
| 별자리 렌더링 | SVG (노드 수백 개 수준) | 글로우 필터·애니메이션에 유리. 노드 1,000개 이상으로 커지면 Canvas/PixiJS 전환 |
| 상태 관리 | React useState/useReducer | 규모상 충분 |
| LLM | Gemini API (gemini-3-flash 계열 — 구현 시점 최신 Flash 모델명 확인) | 경력 추출 + 해석 생성. 사용자가 본인 키를 직접 입력하는 방식 |
| 배포(선택) | 정적 호스팅 (Vercel, GitHub Pages 등) | 서버 불필요 — 브라우저에서 Gemini API 직접 호출 |

### API 키 처리 원칙 (사용자 직접 입력 방식)
- 첫 화면에 API 키 입력창 제공. Google AI Studio에서 무료 발급받는 방법 안내 링크 포함
- 키는 브라우저 localStorage에만 저장. 어떤 서버로도 전송·수집하지 않으며, 이 사실을 입력창 아래에 명시
- 키 미입력 시: 지도 탐색은 가능하되 분석 버튼은 비활성화
- 호출 실패 시 원인 구분 안내: 잘못된 키 / 무료 할당량 초과 / 네트워크 오류
- Gemini API는 브라우저 직접 호출(CORS)을 지원하므로 프록시 서버가 필요 없음

## 4. 핵심 데이터 모델

### 4-1. 소켓 지도 `data/constellation.json`
```json
{
  "clusters": [
    { "id": "strategy", "label": "전략", "position": {"angle": 0, "radius": 280} }
  ],
  "nodes": [
    {
      "id": "brand-strategy",
      "cluster": "strategy",
      "label": "브랜드 전략",
      "keywords": ["브랜드", "포지셔닝", "BI", "리브랜딩"],
      "description": "브랜드의 방향과 정체성을 설계한 경험",
      "position": {"x": 0, "y": 0}
    }
  ],
  "edges": [
    { "from": "brand-strategy", "to": "market-research" }
  ]
}
```

- 클러스터(대분류) 초안: 전략, 창작/기획, 분석/데이터, 리더십/조직, 실행/운영, 학습/자격, 산업 경험, 성과/수상
- 노드는 클러스터당 8~15개, 전체 80~120개로 시작
- 중앙 허브 노드는 "현재의 나"이며 항상 점등 상태
- 지도 자체는 정적 데이터. 모든 사용자가 같은 지도를 공유하고, 점등 상태만 사람마다 다름

### 4-2. 점등 결과 `IlluminationResult`
```json
{
  "nodes": [
    { "id": "brand-strategy", "score": 8, "tier": 3, "evidence": ["DL이앤씨 브랜드 전략 8년"] }
  ]
}
```

## 5. 처리 파이프라인

1. **입력 수집**: 텍스트 입력창(이력서 붙여넣기 또는 자유 서술). 여러 번 나눠 입력하면 누적됨
2. **추출 호출 (Gemini API #1)**: 전체 노드 목록(id, label, keywords)을 시스템 지시문에 넣고, 사용자 서술에서 해당되는 노드와 근거를 JSON으로만 반환하게 함. `responseMimeType: "application/json"`과 responseSchema를 지정해 형식을 강제
3. **점수 계산 (로컬 로직)**:
   - 기본: 매핑된 언급 1회당 +1
   - 가중치: 경력 연차 명시(+2), 정량 성과·수상(+2), 리더 역할(+1)
   - tier 산정: 1~3점 → tier 1(작은 별), 4~7점 → tier 2, 8점 이상 → tier 3(영롱한 별)
4. **렌더링**: tier에 따라 별 크기·광도·색을 달리해 점등. 점등된 노드 사이 간선도 은은하게 발광
5. **해석 호출 (Gemini API #2)**: 점등된 노드·tier·근거를 입력으로, 타로 리딩 문체의 해석문 생성
   - 구성: ① 그대의 아르카나 — 이 커리어를 상징하는 카드 이름 창작(예: "XXII. 다리를 놓는 자") ② 빛나는 별들이 말하는 것(강점 서사) ③ 별들 사이의 어두운 길(아직 잇지 못한 가능성) ④ 다음에 깃들 별(추천 방향 2~3개 — 실제 미점등 노드 id를 지목할 것)
6. **누적 입력 시**: 기존 점수에 합산해 재점등 (별이 자라나는 경험)

## 6. 프롬프트 설계 원칙

- 추출 호출은 "JSON 외 다른 텍스트 절대 금지"를 명시하고, 파싱 실패 시 1회 재시도
- 해석 호출은 감성적이되 근거(evidence) 기반으로 쓰게 할 것 — 없는 경력을 지어내지 않도록 "입력에 없는 사실 창작 금지" 명시
- 두 프롬프트는 `src/prompts/` 폴더에 별도 파일로 분리해 관리

## 7. 폴더 구조

```
career-constellation/
├─ CLAUDE.md
├─ design.md
├─ data/constellation.json
├─ src/
│  ├─ components/   # ConstellationMap, StarNode, EdgeLine, InputPanel, ReadingPanel
│  ├─ prompts/      # extract.js, interpret.js
│  ├─ logic/        # scoring.js, mapping.js
│  ├─ lib/          # gemini.js — API 호출 공통 함수 (키 로드·JSON 파싱·오류 처리)
│  └─ styles/       # design.md의 토큰을 CSS 변수로 구현
```

## 8. 개발 마일스톤

1. **M1 — 지도 뼈대**: constellation.json 초안 + SVG로 미점등 별자리 지도 렌더링
2. **M2 — 수동 점등**: 노드 클릭 시 tier별 점등 확인 (API 없이 시각 검증)
3. **M3 — 추출 연동**: API 키 입력 화면 + 텍스트 입력 → Gemini 추출 → 자동 점등
4. **M4 — 해석 연동**: 별자리 풀이 패널
5. **M5 — 다듬기**: 점등 애니메이션, 누적 입력, 이미지 저장/공유

## 9. 제약 사항

- **저작권**: Diablo·Path of Exile의 스프라이트·이미지 원본 추출 사용 금지. 분위기만 참고하고 모든 시각 자산은 SVG로 자체 제작 (design.md 참조)
- 이력서는 민감 정보이므로 서버에 저장하지 않음. 처리 후 폐기, 결과만 클라이언트에 보관
- API 키를 코드나 저장소에 하드코딩하지 말 것. 키는 오직 「사용자 입력 → localStorage」 경로로만 존재
